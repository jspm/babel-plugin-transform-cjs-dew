var exports = {};
function compile(param, filters, theReporter) {
  return "compiled";
}
(function (compile) {
  compile.compiler = true;
})(compile || (compile = {}));
exports = compile;
export default exports;