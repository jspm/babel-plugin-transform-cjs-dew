console.log(package.version);

for (interface in asdf) {
	console.log(interface);
}

static(x);


// reserved but SHOULDNT change

console.log(null);
eval('');
debugger;
void 0;
class A extends B {
	constructor () {
		console.log(super());
	}
}
